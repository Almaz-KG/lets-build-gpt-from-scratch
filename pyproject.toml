[project]
name = "lets-build-gpt-from-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.3",
    "torch>=2.14.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
