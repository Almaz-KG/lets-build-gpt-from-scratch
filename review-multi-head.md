# Ревью стадии 03, multi-head и feed-forward

Разбор `03_multi_head.py`, 2026-09-17.
Числа посчитаны на этом коде, прогоны по 10 000 шагов, seed 1337.
Правки по ходу ревью были только в проверках, аннотациях, именах и выводе, обучение они не трогают, поэтому числа ниже относятся и к финальной версии файла.

## Одной строкой

Стадия собрана правильно и повторяет видео на этом шаге, но измеритель шумнее, чем половина того, что он измеряет, а самый большой выигрыш на столе лежит не в архитектуре.

---

## Что запуск даёт

Прогон файла: 2.33 train / 2.35 val против 2.46 / 2.48 на одной голове.
Разложение по вкладам, все варианты обучены одинаково, замер на 100 000 токенах вместо 3 200 в самом файле:

| вариант | параметров | train | val |
| --- | --- | --- | --- |
| 02: одна голова 32, без ffwd | 7553 | 2.4360 | 2.4459 |
| 03: восемь голов по 4, без ffwd | 7553 | 2.3216 | 2.3284 |
| 03: одна голова 32 плюс ffwd | 8609 | 2.3604 | 2.3716 |
| 03: восемь по 4 плюс ffwd, как написано | 8609 | 2.2998 | 2.3067 |
| 03: то же плюс residual | 8609 | 2.2800 | 2.2946 |
| 03: как написано, `BATCH_SIZE = 32` | 8609 | 2.1482 | 2.1905 |
| 03: residual и `BATCH_SIZE = 32` | 8609 | 2.1328 | 2.1817 |

**Восемь голов по 4 против одной на 32 это 0.118 лосса при том же числе параметров.**
7553 в обеих строках, потому что `8 * 3 * (32 * 4)` и `3 * (32 * 32)` это одно и то же число 3072.
Не «больше матриц помогло», а специализация: восемь подпространств по 4 измерения учат восемь разных отношений, одно на 32 усредняет их в одно.

**Feed-forward на этой стадии дал 0.022**, причём одной голове он помогает втрое сильнее, чем восьми (0.074 против 0.022).
Это следствие того, что `nn.Linear(n_embd, n_embd)` плюс ReLU без расширения в четыре раза и без обратной проекции - слабейшая версия FFN, и видео её потом меняет.
Отдельно: `ffwd` заканчивается на ReLU, поэтому на вход `lm_head` приходит только неотрицательное - 38.3% значений ровно нули, минимум 0.000, максимум 11.5.

**Residual на этой стадии даёт 0.012, а не 0.095, как на стадии 02.**
Вывод стадии 02 не отменяется, он сужается: разрыв в потоке дорого стоил, пока путь был один, а восемь голов плюс FFN уже дают модели, чем добрать идентичность токена.

---

## Самое дорогое в файле, и это не архитектура

`BATCH_SIZE = 4` стоит **0.116** лосса - столько же, сколько весь переход к восьми головам.
Это число из демонстрационной части видео, где форма тензора показывается на четырёх строчках, чтобы влезть на экран; в обучении там 32.
Стадия 02 несла его же, и там оно уже стояло девятым пунктом в открытых.
Сейчас оно стоит дороже: с ffwd и восемью головами модели есть что учить, а градиент по 32 токенам на шаг слишком шумный, чтобы она это делала.

---

## Измеритель шумнее, чем то, что он измеряет

`estimate_loss(eval_steps=100)` при `BATCH_SIZE = 4` смотрит на 100 × 4 × 8 = **3 200 токенов**.
Двенадцать замеров одной и той же обученной модели, меняется только seed:

| протокол | токенов | среднее | std | разброс |
| --- | --- | --- | --- | --- |
| как в файле: `eval_steps=100`, bs=4 | 3 200 | 2.2902 | 0.0288 | 2.2384 - 2.3253 |
| `eval_steps=200`, bs=64 | 102 400 | 2.3027 | 0.0056 | 2.2959 - 2.3129 |
| `eval_steps=2000`, bs=64 | 1 024 000 | 2.3026 | 0.0013 | 2.3002 - 2.3047 |

Разброс текущего протокола - 0.087.
Вклад feed-forward - 0.022, вклад residual - 0.012.
То есть **обе эти правки лежат ниже шума собственного измерителя**: два прогона одной и той же модели разойдутся сильнее, чем архитектурный шаг между ними.
Это же объясняет скачки в логе обучения - 2.3908 на шаге 5000, 2.4101 на 5500, 2.2374 на 9500.
Модель не разучивается, это дрожит замер.

Пока стадии сравниваются по одной цифре в конце, дальше будет только хуже: следующие шаги видео дают всё меньший прирост на всё большем шуме.

---

## Код: осталось открытым, возможно сознательно

- `BATCH_SIZE = 4`, строка 19, с ценником 0.116.
- `generate` берёт глобальный `BLOCK_SIZE`, строка 125, при живом `block_size` в модели. Два источника одного числа.
- `device=DEVICE` вместо `idx.device`, строка 107.
- `train_` печатает только train loss, строка 151. Расхождение train и val - единственная имеющаяся сигнализация, и она не подключена.
- `train_` и `estimate_loss` зовут модульный `get_batch`, который берёт `BATCH_SIZE` и `BLOCK_SIZE` из глобалей: метод модели ходит наружу и за данными, и за размером батча.
- Восемь `# type: ignore` на файл. Каждый - вопрос «что именно здесь не сходится у чекера», и на примере `mask` видно, что ответ бывает содержательным.
- Порядок аргументов `Head.__init__` поменялся относительно стадии 02: было `(n_embd, head_size, block_size)`, стало `(head_size, n_embd, block_size)`. Само по себе не хуже, но дифф между стадиями показывает перестановку там, где изменения нет.

## Чего в коде правильно нет

Ни у `MultiHeadAttention`, ни у `FeedForward` нет выходной проекции, и FFN не расширяется в четыре раза.
Это не пропуск: на этом шаге видео их там и нет, они приходят вместе с блоками и residual на стадии 04.
Добавить их сейчас - значит стереть дифф между 03 и 04, ради которого стадии и лежат отдельными файлами.

